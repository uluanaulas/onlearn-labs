# 🚀 Пошаговая инструкция деплоя на Render.com

## Шаг 1: Подготовка

Убедитесь, что все файлы на месте:
- ✅ `main.py`
- ✅ `requirements.txt`
- ✅ `Dockerfile`
- ✅ `render.yaml`

## Шаг 2: Git репозиторий

```bash
# Проверьте, что все изменения закоммичены
git status

# Если есть изменения:
git add .
git commit -m "Ready for deployment"
git push
```

## Шаг 3: Деплой на Render.com

### 3.1. Вход на Render
1. Откройте https://render.com
2. Войдите через GitHub
3. Подтвердите доступ к репозиторию

### 3.2. Создание Web Service
1. Нажмите **"New +"** → **"Web Service"**
2. Выберите ваш репозиторий `PCHMI`
3. Нажмите **"Connect"**

### 3.3. Настройки сервиса

**Основные настройки:**
```
Name: onlearn-backend
Region: Singapore (или ближайший)
Branch: main
Root Directory: backend
Runtime: Python 3
```

**Build & Deploy:**
```
Build Command: pip install -r requirements.txt
Start Command: uvicorn main:app --host 0.0.0.0 --port $PORT
```

### 3.4. Переменные окружения

Нажмите **"Advanced"** → **"Add Environment Variable"**:

1. **PYTHON_VERSION**
   - Value: `3.11.0`

2. **FRONTEND_URL** (добавите после деплоя фронтенда)
   - Value: `https://your-frontend.vercel.app`

### 3.5. Создание сервиса

1. Нажмите **"Create Web Service"**
2. Render начнет автоматическую сборку
3. Дождитесь завершения (5-10 минут)

### 3.6. Получение URL

После успешного деплоя вы получите URL:
```
https://onlearn-backend.onrender.com
```

## Шаг 4: Проверка

1. Откройте URL в браузере
2. Должно быть: `"Onlearn API - VERSION 2.0"`
3. Проверьте API Docs: `https://your-backend.onrender.com/docs`
4. Проверьте тестовый эндпоинт: `https://your-backend.onrender.com/test`

## Шаг 5: Обновление CORS (после деплоя фронтенда)

1. В Render Dashboard → Settings → Environment
2. Добавьте/обновите: `FRONTEND_URL` = URL вашего фронтенда
3. Нажмите **"Save Changes"**
4. Render автоматически перезапустит сервис

## ✅ Готово!

Ваш бэкенд задеплоен и готов к использованию!

---

**Примечание:** Render бесплатный план может "засыпать" после 15 минут неактивности. Первый запрос после пробуждения может занять 30-60 секунд.

