# ⚡ Быстрый деплой на Render.com

## 🎯 Все файлы готовы!

Я подготовил все необходимое для деплоя. Вам нужно только выполнить несколько шагов в веб-интерфейсе Render.

## 📋 Чеклист перед деплоем

- [x] ✅ `main.py` - готов
- [x] ✅ `requirements.txt` - готов  
- [x] ✅ `Dockerfile` - готов
- [x] ✅ `render.yaml` - готов
- [x] ✅ CORS настроен для работы с переменными окружения

## 🚀 Шаги деплоя (5 минут)

### 1. Откройте Render.com
https://render.com → Войдите через GitHub

### 2. Создайте Web Service
- Нажмите **"New +"** → **"Web Service"**
- Выберите ваш репозиторий
- Нажмите **"Connect"**

### 3. Заполните форму (скопируйте эти значения):

```
Name: onlearn-backend
Region: Singapore (или любой)
Branch: main
Root Directory: backend
Runtime: Python 3
Build Command: pip install -r requirements.txt
Start Command: uvicorn main:app --host 0.0.0.0 --port $PORT
```

### 4. Добавьте переменные окружения

В разделе **"Environment Variables"** добавьте:

```
PYTHON_VERSION = 3.11.0
```

(FRONTEND_URL добавите позже, после деплоя фронтенда)

### 5. Нажмите "Create Web Service"

Render автоматически:
- Склонирует репозиторий
- Установит зависимости
- Запустит приложение
- Выдаст URL

### 6. Дождитесь деплоя

- Первый деплой: 5-10 минут
- Вы увидите логи в реальном времени
- После успеха получите URL: `https://onlearn-backend.onrender.com`

## ✅ Проверка

1. Откройте URL: `https://your-backend.onrender.com/`
2. Должно быть: `"Onlearn API - VERSION 2.0"`
3. API Docs: `https://your-backend.onrender.com/docs`
4. Тест: `https://your-backend.onrender.com/test`

## 🎉 Готово!

Ваш бэкенд задеплоен и работает!

---

**Важно:** После деплоя фронтенда на Vercel, добавьте в Render:
- `FRONTEND_URL` = URL вашего фронтенда
- Render автоматически перезапустит сервис

