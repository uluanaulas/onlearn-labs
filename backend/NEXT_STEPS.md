# 🚀 Следующие шаги после пуша в репозиторий

## ✅ Что уже сделано

- ✅ Код залит в репозиторий
- ✅ Исправления для продакшена готовы
- ✅ Secure cookies настроены

## 📋 Что делать дальше

### 1. Проверка деплоя на Render.com

Render автоматически подхватит изменения и запустит деплой:

1. **Откройте Render Dashboard:**
   - https://dashboard.render.com
   - Найдите ваш сервис `onlearn-backend`

2. **Проверьте логи деплоя:**
   - Нажмите на сервис
   - Перейдите в "Logs"
   - Убедитесь, что деплой прошел успешно

3. **Дождитесь завершения деплоя:**
   - Обычно занимает 2-5 минут
   - Вы увидите сообщение "Your service is live"

### 2. Проверка после деплоя

#### Проверка бэкенда:

1. **Откройте URL вашего бэкенда:**
   ```
   https://onlearn-backend.onrender.com/
   ```
   Должно быть: `"Onlearn API - VERSION 2.0"`

2. **Проверьте API Docs:**
   ```
   https://onlearn-backend.onrender.com/docs
   ```
   Должна открыться Swagger документация

3. **Проверьте тестовый эндпоинт:**
   ```
   https://onlearn-backend.onrender.com/test
   ```
   Должно вернуть: `{"status": "ok", "message": "Backend работает!"}`

#### Проверка авторизации:

1. **Откройте фронтенд** (если задеплоен)

2. **Попробуйте войти:**
   - Email: `alice@example.com`
   - Password: `password123`

3. **Проверьте в DevTools (F12):**
   - Application → Cookies
   - Cookie `session_user_id` должна быть:
     - ✅ `HttpOnly` 
     - ✅ `Secure` (для HTTPS)
     - ✅ `SameSite=Lax`

4. **Проверьте запросы:**
   - Network → попробуйте открыть профиль
   - `/me` должен вернуть 200
   - `/my-courses` должен вернуть 200 (не 401!)

### 3. Настройка переменных окружения (если еще не сделано)

В Render Dashboard → Settings → Environment Variables:

1. **FRONTEND_URL** (если еще не добавлен)
   - Key: `FRONTEND_URL`
   - Value: `https://your-frontend.vercel.app` (URL вашего фронтенда)

2. После добавления перезапустите сервис:
   - Manual Deploy → Clear build cache & deploy

### 4. Если что-то не работает

#### Проблема: Cookies не работают

**Решение:**
- Проверьте, что `FRONTEND_URL` правильно настроен в Render
- Убедитесь, что фронтенд и бэкенд на HTTPS
- Проверьте CORS в логах Render

#### Проблема: 401 ошибки

**Решение:**
- Проверьте логи Render на наличие ошибок
- Убедитесь, что cookie установлена (DevTools → Cookies)
- Попробуйте выйти и войти заново

#### Проблема: Деплой не прошел

**Решение:**
- Проверьте логи деплоя в Render
- Убедитесь, что все зависимости в `requirements.txt`
- Проверьте, что `main.py` находится в `backend/`

## 🎯 Чеклист после деплоя

- [ ] Render показывает "Your service is live"
- [ ] Backend URL возвращает правильный ответ
- [ ] API Docs открывается
- [ ] Можно войти через фронтенд
- [ ] Cookies установлены правильно (Secure, HttpOnly)
- [ ] `/me` работает
- [ ] `/my-courses` работает (нет 401!)
- [ ] Все функции работают стабильно

## 📞 Полезные ссылки

- Render Dashboard: https://dashboard.render.com
- Ваш Backend: https://onlearn-backend.onrender.com (замените на ваш URL)
- API Docs: https://onlearn-backend.onrender.com/docs

---

**Готово!** После успешного деплоя все должно работать корректно. 🎉

