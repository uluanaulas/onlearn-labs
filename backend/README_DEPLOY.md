# 🚀 Деплой приложения Onlearn

## 📋 Содержание
1. [Локальный запуск с Docker](#локальный-запуск-с-docker)
2. [Деплой на Render.com (Backend)](#деплой-на-rendercom-backend)
3. [Деплой на Vercel.com (Frontend)](#деплой-на-vercelcom-frontend)
4. [Проверка деплоя](#проверка-деплоя)

---

## 🐳 Локальный запуск с Docker

### Требования
- Docker Desktop (или Docker + Docker Compose)
- Git

### Быстрый старт

```bash
# Из корня проекта (где находится docker-compose.yml)
docker-compose up --build

# В фоновом режиме
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

**Доступ:**
- Frontend: http://localhost:8080
- Backend: http://localhost:8000
- API Docs: http://localhost:8000/docs

### Отдельный запуск только бэкенда

```bash
# Из директории backend
docker build -t onlearn-backend .
docker run -p 8000:8000 -v $(pwd)/app.db:/app/app.db onlearn-backend
```

---

## ☁️ Деплой на Render.com (Backend)

### Шаг 1: Подготовка репозитория

1. Убедитесь, что все изменения закоммичены:
```bash
git add .
git commit -m "Add deployment configs"
git push
```

2. Проверьте наличие файлов:
   - `Dockerfile` (в корне backend/)
   - `requirements.txt`
   - `main.py`

### Шаг 2: Создание сервиса на Render

1. **Зайдите на https://render.com**
   - Войдите через GitHub
   - Подтвердите доступ к репозиторию

2. **Создайте новый Web Service:**
   - Нажмите "New +" → "Web Service"
   - Выберите ваш репозиторий

3. **Настройки сервиса:**
   ```
   Name: onlearn-backend
   Region: Singapore (или ближайший к вам)
   Branch: main
   Root Directory: backend
   Runtime: Python 3
   Build Command: pip install -r requirements.txt
   Start Command: uvicorn main:app --host 0.0.0.0 --port $PORT
   ```

4. **Переменные окружения (Environment Variables):**
   - `PYTHON_VERSION` = `3.11.0`
   - `FRONTEND_URL` = `https://your-frontend.vercel.app` (добавите после деплоя фронтенда)

5. **Нажмите "Create Web Service"**

### Шаг 3: Ожидание деплоя

- Render автоматически начнет сборку
- Первый деплой может занять 5-10 минут
- После успешного деплоя вы получите URL: `https://onlearn-backend.onrender.com`

### Шаг 4: Обновление CORS

После получения URL бэкенда, обновите `main.py`:

```python
# В main.py уже настроено чтение из переменной окружения
# Просто добавьте FRONTEND_URL в Render:
# Settings → Environment → Add Environment Variable
# Key: FRONTEND_URL
# Value: https://your-frontend.vercel.app
```

---

## 🚀 Деплой на Vercel.com (Frontend)

### Вариант 1: Через веб-интерфейс (Рекомендуется)

1. **Зайдите на https://vercel.com**
   - Войдите через GitHub
   - Подтвердите доступ к репозиторию

2. **Импорт проекта:**
   - Нажмите "Add New..." → "Project"
   - Выберите ваш репозиторий

3. **Настройки проекта:**
   ```
   Framework Preset: Vite
   Root Directory: frontend
   Build Command: npm run build (автоматически)
   Output Directory: dist (автоматически)
   Install Command: npm install (автоматически)
   ```

4. **Переменные окружения:**
   - Нажмите "Environment Variables"
   - Добавьте:
     ```
     Name: VITE_API_URL
     Value: https://your-backend.onrender.com
     ```
   - Выберите все окружения (Production, Preview, Development)

5. **Нажмите "Deploy"**

### Вариант 2: Через CLI

```bash
cd frontend
npm install -g vercel
vercel login
vercel
```

При запросе:
- Set up and deploy? **Y**
- Which scope? Выберите ваш аккаунт
- Link to existing project? **N**
- Project name? `onlearn-frontend`
- Directory? `./`
- Override settings? **N**

Добавьте переменную окружения:
```bash
vercel env add VITE_API_URL
# Введите: https://your-backend.onrender.com
```

---

## ✅ Проверка деплоя

### Backend (Render.com)

1. **Проверка доступности:**
   ```bash
   curl https://your-backend.onrender.com/
   # Должно вернуть: "Onlearn API - VERSION 2.0"
   ```

2. **Проверка API:**
   - Откройте: `https://your-backend.onrender.com/docs`
   - Должна открыться Swagger документация

3. **Проверка эндпоинта:**
   ```bash
   curl https://your-backend.onrender.com/test
   # Должно вернуть: {"status": "ok", "message": "Backend работает!"}
   ```

### Frontend (Vercel.com)

1. **Откройте URL:** `https://your-frontend.vercel.app`
2. **Проверьте:**
   - Страница загружается
   - Можно войти в аккаунт
   - API запросы работают (проверьте Network в DevTools)

### Интеграция

1. **Обновите CORS на бэкенде:**
   - В Render: Settings → Environment
   - Добавьте: `FRONTEND_URL` = `https://your-frontend.vercel.app`
   - Перезапустите сервис

2. **Проверьте работу:**
   - Войдите на фронтенде
   - Проверьте, что данные загружаются
   - Попробуйте создать комментарий

---

## 🔧 Устранение проблем

### Backend не запускается на Render

**Проблема:** Ошибка при сборке
- Проверьте логи в панели Render
- Убедитесь, что `requirements.txt` содержит все зависимости
- Проверьте, что `main.py` находится в `backend/`

**Проблема:** 502 Bad Gateway
- Проверьте логи: Render Dashboard → Logs
- Убедитесь, что приложение слушает на `0.0.0.0` и порту `$PORT`
- Проверьте команду запуска: `uvicorn main:app --host 0.0.0.0 --port $PORT`

### Frontend не подключается к Backend

**Проблема:** CORS ошибки
- Убедитесь, что `FRONTEND_URL` добавлен в переменные окружения Render
- Проверьте, что URL совпадает точно (с https://)
- Перезапустите бэкенд после изменения переменных

**Проблема:** 401 Unauthorized
- Проверьте, что `VITE_API_URL` правильно настроен в Vercel
- Убедитесь, что cookies работают (проверьте в DevTools → Application → Cookies)

### Cookies не работают

**Проблема:** Сессия не сохраняется
- Проверьте, что `allow_credentials=True` в CORS
- Убедитесь, что домены правильно настроены
- Проверьте SameSite атрибут (для разных доменов может потребоваться `None`)

---

## 📝 Структура файлов для деплоя

```
PCHMI/
├── backend/
│   ├── Dockerfile              # Docker образ для бэкенда
│   ├── .dockerignore           # Игнорируемые файлы
│   ├── main.py                 # Основной файл приложения
│   ├── requirements.txt        # Python зависимости
│   └── render.yaml            # Конфигурация Render (опционально)
├── frontend/
│   ├── Dockerfile              # Docker образ для фронтенда
│   ├── vercel.json            # Конфигурация Vercel
│   └── ...
├── docker-compose.yml          # Локальный запуск всего приложения
└── README_DEPLOY.md           # Эта инструкция
```

---

## 🎯 Чеклист для демонстрации

- [ ] Локальный запуск с Docker работает
- [ ] Backend задеплоен на Render.com
- [ ] Frontend задеплоен на Vercel.com
- [ ] CORS настроен правильно
- [ ] Логин работает на продакшене
- [ ] Комментарии работают
- [ ] Все функции приложения работают

---

## 📞 Полезные ссылки

- Render.com Dashboard: https://dashboard.render.com
- Vercel Dashboard: https://vercel.com/dashboard
- Docker Documentation: https://docs.docker.com
- FastAPI Documentation: https://fastapi.tiangolo.com
