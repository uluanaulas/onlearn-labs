# 🔐 Инструкция по пушу в репозиторий

## ✅ Что сделано

Все изменения закоммичены локально:
- Коммит: `c0f5776`
- Сообщение: "Add deployment configuration: Docker, Render.com, Vercel setup"
- 29 файлов добавлено

## ⚠️ Проблема

Ошибка доступа к GitHub репозиторию. Нужно настроить аутентификацию.

## 🔧 Решения

### Вариант 1: GitHub Personal Access Token (Рекомендуется)

1. **Создайте токен:**
   - Зайдите на https://github.com/settings/tokens
   - Нажмите "Generate new token (classic)"
   - Выберите scope: `repo` (полный доступ к репозиториям)
   - Скопируйте токен

2. **Используйте токен для пуша:**
   ```bash
   git push https://<TOKEN>@github.com/ulyanaulasen/onlearn-labs.git main
   ```

### Вариант 2: GitHub Desktop

1. Установите GitHub Desktop
2. Откройте репозиторий
3. Нажмите "Push origin"

### Вариант 3: Через веб-интерфейс GitHub

1. Зайдите на https://github.com/ulyanaulasen/onlearn-labs
2. Нажмите "Upload files"
3. Перетащите все файлы из локальной папки
4. Сделайте коммит

### Вариант 4: Настроить SSH

1. **Создайте SSH ключ:**
   ```bash
   ssh-keygen -t ed25519 -C "your_email@example.com"
   ```

2. **Добавьте ключ в GitHub:**
   - Скопируйте содержимое `~/.ssh/id_ed25519.pub`
   - GitHub → Settings → SSH and GPG keys → New SSH key

3. **Измените remote на SSH:**
   ```bash
   git remote set-url onlearn-labs git@github.com:ulyanaulasen/onlearn-labs.git
   git push --set-upstream onlearn-labs main
   ```

## 📋 Что будет запушено

- ✅ Dockerfile
- ✅ docker-compose.yml
- ✅ render.yaml
- ✅ Все конфигурации для деплоя
- ✅ main.py
- ✅ requirements.txt
- ✅ Документация

## 🎯 После успешного пуша

После того как код будет в репозитории, можно:
1. Задеплоить на Render.com (он автоматически подключится к GitHub)
2. Задеплоить фронтенд на Vercel.com

