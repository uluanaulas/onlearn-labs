version: '3.8'

services:
  backend:
    build: .
    container_name: onlearn-backend
    ports:
      - "8000:8000"
    volumes:
      - ./app.db:/app/app.db
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

