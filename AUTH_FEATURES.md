# Новый функционал: Аутентификация и запись на курсы

## ✅ Что добавлено

### Backend (FastAPI)

1. **Аутентификация с JWT токенами**
   - Эндпоинт `/login` - вход в систему
   - Эндпоинт `/me` - получение информации о текущем пользователе
   - Защита эндпоинтов через JWT токены

2. **Обновленная модель User**
   - Добавлено поле `email` (уникальное)
   - Добавлено поле `hashed_password` (хэшированные пароли)

3. **Обновленные эндпоинты**
   - `POST /enroll` - теперь использует текущего пользователя (не требует user_id)
   - `GET /my-courses` - получение курсов текущего пользователя (вместо `/users/{id}/courses`)

### Frontend (React)

1. **Страница входа** (`/login`)
   - Форма входа с email и паролем
   - Показывает тестовые аккаунты

2. **Контекст аутентификации** (`AuthContext`)
   - Управление состоянием пользователя
   - Сохранение токена в localStorage
   - Автоматическая проверка токена при загрузке

3. **Обновленная страница профиля** (`/profile`)
   - Показывает данные текущего пользователя
   - Загружает записанные курсы из API
   - Отображает прогресс по каждому курсу
   - Кнопка выхода

4. **Обновленная страница курса** (`/course/:id`)
   - Кнопка "Записаться на курс" работает с авторизацией
   - Если не авторизован - перенаправляет на страницу входа

5. **Обновленный Header**
   - Показывает кнопку "Войти", если не авторизован
   - Показывает иконку профиля, если авторизован

## 🔑 Тестовые аккаунты

Все пользователи имеют пароль: `password123`

- **Студент 1**: alice@example.com / password123
- **Студент 2**: bob@example.com / password123
- **Преподаватель**: sarah@example.com / password123
- **Админ**: admin@example.com / password123

## 📝 Как использовать

### 1. Вход в систему

1. Откройте http://localhost:8080/login
2. Введите email и пароль
3. После успешного входа вы будете перенаправлены в профиль

### 2. Запись на курс

1. Войдите в систему
2. Перейдите на страницу любого курса (например, http://localhost:8080/course/1)
3. Нажмите кнопку "Записаться на курс"
4. Курс появится в вашем профиле

### 3. Просмотр записанных курсов

1. Войдите в систему
2. Перейдите в профиль: http://localhost:8080/profile
3. Во вкладке "Мои курсы" вы увидите все записанные курсы с прогрессом

## 🔧 Технические детали

### Backend API

**POST /login**
```json
// Request (form-data)
username: "alice@example.com"
password: "password123"

// Response
{
  "access_token": "eyJ...",
  "token_type": "bearer",
  "user": {
    "id": 1,
    "name": "Alice Johnson",
    "email": "alice@example.com",
    "role": "student"
  }
}
```

**GET /me** (требует авторизации)
```json
// Headers
Authorization: Bearer <token>

// Response
{
  "id": 1,
  "name": "Alice Johnson",
  "email": "alice@example.com",
  "role": "student"
}
```

**POST /enroll** (требует авторизации)
```json
// Request
{
  "course_id": 1
}

// Response
{
  "id": 1,
  "user_id": 1,
  "course_id": 1,
  "progress_percent": 0
}
```

**GET /my-courses** (требует авторизации)
```json
// Response
[
  {
    "enrollment_id": 1,
    "course": {
      "id": 1,
      "title": "Веб-разработка для начинающих...",
      ...
    },
    "progress_percent": 45
  }
]
```

### Frontend

**Использование AuthContext:**
```typescript
import { useAuth } from '@/contexts/AuthContext';

const MyComponent = () => {
  const { user, isAuthenticated, login, logout } = useAuth();
  
  // ...
};
```

**API функции:**
```typescript
import { login, enrollCourse, getMyCourses } from '@/lib/api';

// Вход
await login('alice@example.com', 'password123');

// Запись на курс
await enrollCourse(courseId);

// Получение моих курсов
const courses = await getMyCourses();
```

## 🚀 Запуск

1. **Backend** (уже запущен):
   ```powershell
   cd backend
   .venv\Scripts\Activate.ps1
   python -m uvicorn main:app --reload
   ```

2. **Frontend** (уже запущен):
   ```powershell
   cd frontend
   npm run dev
   ```

## ⚠️ Важно

- Токены хранятся в `localStorage` браузера
- Токены действительны 30 минут
- При истечении токена нужно войти заново
- База данных будет пересоздана при следующем запуске (старая БД удалена для добавления новых полей)

## 📋 Чеклист для тестирования

- [ ] Вход в систему с тестовым аккаунтом
- [ ] Просмотр профиля с данными пользователя
- [ ] Запись на курс со страницы курса
- [ ] Просмотр записанных курсов в профиле
- [ ] Выход из системы
- [ ] Попытка записи на курс без авторизации (должна перенаправить на вход)


