# Быстрый запуск для демонстрации

## 🚀 Запуск для показа преподу

### Шаг 1: Запустите бэкенд

Откройте терминал в папке `backend`:

```powershell
# Активируйте виртуальное окружение
.venv\Scripts\Activate.ps1

# Запустите сервер
python -m uvicorn main:app --reload
```

✅ Бэкенд будет работать на: **http://localhost:8000**

### Шаг 2: Запустите фронтенд

Откройте **новый** терминал в папке `frontend`:

```powershell
# Установите зависимости (если еще не установлены)
npm install

# Запустите dev сервер
npm run dev
```

✅ Фронтенд будет работать на: **http://localhost:8080**

### Шаг 3: Откройте в браузере

1. **Фронтенд**: http://localhost:8080/courses
   - Здесь вы увидите курсы из базы данных
   
2. **Swagger UI**: http://localhost:8000/docs
   - Интерактивная документация API
   - Можно тестировать эндпоинты прямо в браузере

## 📋 Что показывать преподу

1. **Фронтенд с реальными данными**
   - Откройте http://localhost:8080/courses
   - Покажите, что курсы загружаются из бэкенда
   - Откройте любой курс - данные тоже из API

2. **Swagger UI**
   - Откройте http://localhost:8000/docs
   - Покажите все эндпоинты
   - Протестируйте GET /courses прямо в браузере

3. **База данных**
   - Покажите файл `backend/app.db` (SQLite)
   - Объясните, что данные сохраняются между запусками

## 🔧 Если что-то не работает

### Бэкенд не запускается
- Убедитесь, что виртуальное окружение активировано
- Проверьте: `python --version` (должен быть Python 3.10+)
- Переустановите зависимости: `pip install -r requirements.txt`

### Фронтенд не подключается к бэкенду
- Убедитесь, что бэкенд запущен на порту 8000
- Проверьте консоль браузера (F12) на ошибки
- Убедитесь, что CORS настроен (в `main.py`)

### База данных пустая
- Удалите файл `backend/app.db`
- Перезапустите бэкенд
- Данные автоматически загрузятся

## 🌐 Деплой на реальный сервер

**Для учебного проекта деплой НЕ обязателен!**

Можно показать работу локально. Но если нужен онлайн:

### Варианты бесплатного хостинга:

**Backend:**
- Render.com - https://render.com
- Railway.app - https://railway.app

**Frontend:**
- Vercel - https://vercel.com
- Netlify - https://netlify.com

Подробные инструкции в файле `INTEGRATION.md`

## ✅ Чеклист перед демонстрацией

- [ ] Бэкенд запущен и отвечает на http://localhost:8000
- [ ] Фронтенд запущен и открывается на http://localhost:8080
- [ ] Курсы отображаются на странице /courses
- [ ] Swagger UI открывается на /docs
- [ ] База данных содержит данные (файл app.db существует)

## 📝 Полезные команды

```powershell
# Проверить, что бэкенд работает
curl http://localhost:8000/

# Проверить курсы
curl http://localhost:8000/courses

# Запустить тестовый скрипт
.\test_backend.ps1
```


